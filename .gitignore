# Node
node_modules/
npm-debug.log*
.npm

# Next.js
.next/
out/

# Build artifacts
viewer/public/graph.json
# embeddings.json은 빌드 캐시 역할도 겸함 — commit하면 incremental build로 비용 절감
# (commit 안 하면 매 빌드마다 전체 재계산. 노드 수 많을수록 비용 차이 큼)
# viewer/public/embeddings.json

# Env
.env
.env.local
.env*.local

# IDE/OS
.DS_Store
.vscode/
.idea/

# Jest/Vitest
coverage/

# 임시 sanity check files (work scope 빌드 검증용)
wiki/work/**/*-temp-sanity.md
